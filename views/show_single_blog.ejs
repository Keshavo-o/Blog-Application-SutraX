<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><% if (locals.blog) { %><%= blog.title %><% } %> | SutraX</title>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Noto+Serif+Devanagari:wght@700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #000;
      color: #e6e6e6;
      margin: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Navbar */
    .navbar {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }
    .navbar-brand {
      font-family: 'Noto Serif Devanagari', serif;
      font-size: 1.8rem;
      font-weight: bold;
      color: #ffffff !important;
      letter-spacing: 1px;
    }
    .nav-link {
      font-weight: 500;
      color: #e6e6e6 !important;
      margin: 0 0.5rem;
      transition: color 0.2s;
    }
    .nav-link:hover {
      color: #1D9BF0 !important;
    }

    /* Glass Card */
    .glass-card {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 20px;
      padding: 2.5rem 2rem;
      width: 60%;
      min-width: 400px;
      margin: 10vh auto;
      box-shadow: 0 8px 32px rgba(0,0,0,0.4);
      animation: fadeIn 1.2s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .blog-title {
      font-family: 'Noto Serif Devanagari', serif;
      font-size: 2rem;
      color: #1D9BF0;
      margin-bottom: 1rem;
      text-align: center;
      text-shadow: 0 2px 8px rgba(29,155,240,0.4);
    }

    .blog-meta {
      font-size: 0.9rem;
      color: #aaa;
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .blog-image {
      width: 100%;
      max-height: 400px;
      object-fit: cover;
      border-radius: 16px;
      margin-bottom: 1.5rem;
    }

    .blog-content {
      font-size: 1rem;
      line-height: 1.8;
      color: #e6e6e6;
      white-space: pre-line;
      margin-bottom: 2rem;
    }

    .btn-back {
      display: block;
      margin: 2rem auto 0;
      background-color: #1D9BF0;
      border: none;
      border-radius: 12px;
      padding: 0.7rem 1.5rem;
      font-weight: 600;
      text-align: center;
      transition: all 0.3s ease;
      color: #fff;
      text-decoration: none;
    }
    .btn-back:hover {
      background-color: #1a8cd8;
      transform: scale(1.03);
      box-shadow: 0 4px 20px rgba(29,155,240,0.35);
    }

    /* Comments Section */
    .comments-section {
      margin-top: 3rem;
    }
    .comment {
      background: rgba(255, 255, 255, 0.05);
      padding: 1rem 1.2rem;
      border-radius: 12px;
      margin-bottom: 1rem;
      border: 1px solid rgba(255,255,255,0.08);
    }
    .comment-author {
      font-weight: 600;
      color: #1D9BF0;
    }
    .comment-date {
      font-size: 0.8rem;
      color: #aaa;
    }
    .comment-text {
      margin-top: 0.5rem;
      font-size: 0.95rem;
      line-height: 1.6;
    }

    /* Comment Form */
    .comment-form {
      margin-top: 2rem;
      padding: 1.5rem;
      background: rgba(255,255,255,0.03);
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.08);
    }
    .comment-form textarea {
      resize: none;
      border-radius: 10px;
    }
    .btn-submit {
      margin-top: 1rem;
      background-color: #1D9BF0;
      border: none;
      border-radius: 10px;
      padding: 0.6rem 1.2rem;
      font-weight: 600;
      color: #fff;
      transition: 0.3s;
    }
    .btn-submit:hover {
      background-color: #1a8cd8;
      transform: scale(1.02);
      box-shadow: 0 4px 15px rgba(29,155,240,0.35);
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-custom fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">सूत्रX - SutraX</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/blogs">Blogs</a></li>
          <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
          <li class="nav-item"><a class="nav-link" href="/userprofile">Profile</a></li>  
          <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Blog Card -->
  <% if(locals.blog) { %>
  <div class="glass-card">
    <div class="blog-title"><%= blog.title %></div>
    <div class="blog-meta">By 
      <a target="_blank" href="/users/<%= blog.author %>"><%= blog.author %></a> | 
      <%= new Date(blog.createdAt).toLocaleDateString() %>
    </div>

    <% if(blog.image){ %>
      <img src="<%= blog.image %>" alt="Blog Image" class="blog-image">
    <% } %>

    <div class="blog-content"><%= blog.body %></div>

    <!-- Comments Section -->
    <div class="comments-section">
      <h5>Comments</h5>
      <% if (locals.comments && comments.length > 0) { %>
        <% comments.forEach(comment => { %>
          <div class="comment">
            <div class="d-flex justify-content-between">
              <span class="comment-author"><%= comment.author %></span>
              <span class="comment-date"><%= comment.createdAt.toLocaleDateString() %></span>
            </div>
            <div class="comment-text"><%= comment.body %></div>
          </div>
        <% }) %>
      <% } else { %>
        <p class="text-muted">No comments yet. Be the first to comment!</p>
      <% } %>

      <!-- Comment Form -->
      <form class="comment-form" method="POST" action="/post_comment">
        <div class="mb-3">
          <label for="postId" class="form-label">Sutra Id</label>
          <input type="text" class="form-control" value="<%= blog._id %>" readonly id="postId" name="postId" required>
        </div>
        <div class="mb-3">
          <label for="author" class="form-label">Your Name</label>
          <input type="text" class="form-control" id="author" name="author" required>
        </div>
        <div class="mb-3">
          <label for="text" class="form-label">Comment</label>
          <textarea class="form-control" id="text" name="text" rows="3" required></textarea>
        </div>
        <button type="submit" class="btn-submit">Submit Comment</button>
      </form>
    </div>

    <a href="/" class="btn-back">Back to Home</a>
  </div>
  <% } %>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
