<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Blog | SutraX</title>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Noto+Serif+Devanagari:wght@700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #000;
      color: #e6e6e6;
      margin: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Navbar */
    .navbar {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }
    .navbar-brand {
      font-family: 'Noto Serif Devanagari', serif;
      font-size: 1.8rem;
      font-weight: bold;
      color: #ffffff !important;
      letter-spacing: 1px;
    }
    .nav-link {
      font-weight: 500;
      color: #e6e6e6 !important;
      margin: 0 0.5rem;
      transition: color 0.2s;
    }
    .nav-link:hover {
      color: #1D9BF0 !important;
    }

    /* Glass Card Form */
  .glass-card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 20px;
  padding: 3rem 2.5rem;
  width: 50%;              /* Changed from max-width to 50% */
  min-width: 400px;        /* Optional: ensures it doesn't get too small on small screens */
  margin: 10vh auto;
  box-shadow: 0 8px 32px rgba(0,0,0,0.4);
  animation: fadeIn 1.2s ease;
}

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .form-title {
      font-family: 'Noto Serif Devanagari', serif;
      font-size: 2rem;
      color: #1D9BF0;
      margin-bottom: 1.5rem;
      text-align: center;
      text-shadow: 0 2px 8px rgba(29,155,240,0.4);
    }

    .form-label {
      font-weight: 600;
      margin-top: 1rem;
    }

    .form-control {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.2);
      color: #e6e6e6;
      border-radius: 12px;
    }
    .form-control:focus {
      background: rgba(255,255,255,0.08);
      border-color: #1D9BF0;
      color: #fff;
      box-shadow: 0 0 10px rgba(29,155,240,0.4);
    }

    .btn-primary {
      background-color: #1D9BF0;
      border: none;
      border-radius: 12px;
      font-weight: 600;
      padding: 0.8rem 1.5rem;
      margin-top: 1.5rem;
      transition: all 0.3s ease;
      display: block;
      width: 100%;
    }
    .btn-primary:hover {
      background-color: #1a8cd8;
      transform: scale(1.03);
      box-shadow: 0 4px 20px rgba(29,155,240,0.35);
    }

    .note {
      font-size: 0.85rem;
      color: #aaa;
      margin-top: 0.3rem;
    }
    .form-control::placeholder {
  color: #aaa;
}
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-custom fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">सूत्रX - SutraX</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/userprofile">Profile</a>
          </li>  
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Add Blog Form -->
  <div class="glass-card">
    <div class="form-title">Add New Sutra</div>
    <form action="/add_blog" method="POST" enctype="multipart/form-data">
        <!-- This is comment enctype="multipart/form-data" -->
        <div class="mb-3">
        <label for="title" class="form-label">CreatedBy</label>
        <input type="text" class="form-control" id="title" name="username" placeholder="Enter your name" value="<%if(locals.user.username){%><%= user.username %><% } %>" readonly required>
      </div>
      <div class="mb-3">
        <label for="title" class="form-label">Title</label>
        <input type="text" class="form-control" id="title" name="title" placeholder="Enter title for your Sutra" required>
      </div>

      <div class="mb-3">
        <label for="description" class="form-label">Sutra</label>
        <textarea class="form-control" maxlength="1000" id="description" name="description" rows="6" placeholder="Write your content here..." required></textarea>
        <div class="note">Max-length: 1000 words</div>

      </div>

      <div class="mb-3">
        <label for="image" class="form-label">Upload Image (optional)</label>
        <input class="form-control" type="file" id="image" name="image" required accept="image/*">
        <div class="note">Supported formats: JPG, PNG, GIF. Max size: 5MB</div>
      </div>

      <button type="submit" class="btn btn-primary">Publish Sutra</button>
    </form>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
